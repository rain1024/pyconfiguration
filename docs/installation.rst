.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install python_configuration

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python_configuration
    $ pip install python_configuration
